import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter } from "expo-router";
import { useMemo, useState } from "react";
import { Modal, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";

import { ScreenContainer } from "@/components/screen-container";
import { Card, Eyebrow, IconBadge, Pill, PrimaryButton, SectionTitle, palette, styles as ui, impact } from "@/components/rematter-ui";

const listings = [
  { id: "RM-CIT-2026-000481", material: "Citrus-derived feedstock", type: "Biomass", quantity: "1,240 kg", location: "Ahmedabad", status: "Quality verified", tag: "For technical evaluation", color: palette.mint, icon: "eco" as const },
  { id: "RM-OIL-2026-000219", material: "Recovered cooking-oil feedstock", type: "Liquid stream", quantity: "4,800 L", location: "Mumbai", status: "Processed · docs ready", tag: "Specification listed", color: palette.amber, icon: "opacity" as const },
  { id: "RM-COF-2026-000092", material: "Spent coffee grounds", type: "Biomass", quantity: "620 kg", location: "Bengaluru", status: "Testing in progress", tag: "Sample available", color: palette.blue, icon: "local-cafe" as const },
];

export default function MarketplaceScreen() {
  const router = useRouter();
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("All materials");
  const [requested, setRequested] = useState<string | null>(null);
  const [selected, setSelected] = useState<(typeof listings)[number] | null>(null);
  const filters = ["All materials", "Biomass", "Liquid stream"];
  const visible = useMemo(() => listings.filter((item) => (filter === "All materials" || item.type === filter) && item.material.toLowerCase().includes(query.toLowerCase())), [filter, query]);

  return (
    <ScreenContainer className="p-0">
      <ScrollView contentContainerStyle={local.content} showsVerticalScrollIndicator={false}>
        <View style={local.topbar}><Pressable onPress={() => router.back()} style={({ pressed }) => [local.back, pressed && ui.pressed]}><MaterialIcons name="arrow-back" size={20} color={palette.text} /></Pressable><View><Eyebrow>RE:MATTER EXCHANGE</Eyebrow><Text style={local.title}>Verified supply</Text></View><View style={local.topIcon}><MaterialIcons name="notifications-none" size={20} color={palette.mint} /></View></View>
        <Card tone="blue" style={local.hero}><View style={{ flex: 1 }}><Eyebrow color={palette.blue}>MANUFACTURER VIEW</Eyebrow><Text style={local.heroTitle}>Procure with a material passport.</Text><Text style={local.heroText}>Traceable recovered feedstock, clearly labeled for evaluation.</Text></View><IconBadge name="inventory-2" color={palette.blue} background="rgba(120,184,255,0.12)" size={56} /></Card>
        <View style={local.search}><MaterialIcons name="search" size={20} color={palette.muted} /><TextInput value={query} onChangeText={setQuery} placeholder="Search material, city, batch ID" placeholderTextColor={palette.muted} style={local.searchInput} /></View>
        <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={local.filterRow}>{filters.map((item) => <Pressable key={item} onPress={() => { impact(); setFilter(item); }} style={[local.filter, filter === item && local.filterActive]}><Text style={[local.filterText, filter === item && local.filterTextActive]}>{item}</Text></Pressable>)}</ScrollView>
        <SectionTitle title={`${visible.length} available materials`} action="My RFQs" onAction={() => setSelected(listings[0])} />
        {visible.length === 0 ? <Card style={local.empty}><IconBadge name="search-off" color={palette.muted} background="rgba(156,181,180,0.1)" /><Text style={local.emptyTitle}>No matching material</Text><Text style={local.emptyText}>Try another material or reset the filter.</Text></Card> : visible.map((item) => <Card key={item.id} style={local.listing}><View style={local.listingTop}><View style={local.listingName}><IconBadge name={item.icon} color={item.color} background={`${item.color}18`} size={44} /><View><Text style={local.materialName}>{item.material}</Text><Text style={local.muted}>{item.type} · {item.location}</Text></View></View><Pill label={item.status.split(" · ")[0]} color={item.color} background={`${item.color}18`} /></View><View style={local.quantityRow}><View><Text style={local.quantity}>{item.quantity}</Text><Text style={local.muted}>available quantity</Text></View><View style={local.batchBlock}><Text style={local.batchLabel}>BATCH</Text><Text style={local.batch}>{item.id}</Text></View></View><View style={local.divider} /><View style={local.listingFoot}><View><Text style={local.muted}>Positioning</Text><Text style={local.disclaimer}>{item.tag}</Text></View><View style={local.actionButtons}><Pressable onPress={() => setSelected(item)} style={local.passportButton}><MaterialIcons name="qr-code-2" size={16} color={palette.mint} /><Text style={local.passportText}>Passport</Text></Pressable><PrimaryButton label={requested === item.id ? "Requested" : "Request sample"} onPress={() => setRequested(item.id)} icon={requested === item.id ? "check" : "arrow-forward"} small /></View></View></Card>)}
        <Text style={local.note}>Supply figures are demo data. Quality and end-use claims are limited to the listed status and specification.</Text>
      </ScrollView>
      <Modal visible={Boolean(selected)} transparent animationType="slide" onRequestClose={() => setSelected(null)}><View style={local.modalBackdrop}><View style={local.modal}><View style={local.modalHead}><Eyebrow>MATERIAL PASSPORT</Eyebrow><Pressable onPress={() => setSelected(null)}><MaterialIcons name="close" size={20} color={palette.muted} /></Pressable></View>{selected ? <><Text style={local.modalTitle}>{selected.material}</Text><Text style={local.modalId}>{selected.id}</Text><View style={local.passportGrid}><View><Text style={local.muted}>Origin</Text><Text style={local.value}>{selected.location}</Text></View><View><Text style={local.muted}>Quantity</Text><Text style={local.value}>{selected.quantity}</Text></View><View><Text style={local.muted}>Collection</Text><Text style={local.value}>Completed</Text></View><View><Text style={local.muted}>Testing</Text><Text style={local.value}>{selected.status}</Text></View></View><View style={local.timeline}>{["SOURCE", "COLLECTED", "AGGREGATED", "PROCESSED", "AVAILABLE"].map((step, index) => <View key={step} style={local.timelineStep}><View style={[local.timelineDot, index < 4 && local.timelineDone]} /><Text style={local.timelineText}>{step}</Text></View>)}</View><PrimaryButton label="Request technical sample" onPress={() => { setRequested(selected.id); setSelected(null); }} icon="science" /></> : null}</View></View></Modal>
    </ScreenContainer>
  );
}

const local = StyleSheet.create({ content: { padding: 20, paddingBottom: 42, gap: 14 }, topbar: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", marginBottom: 4 }, back: { width: 36, height: 36, alignItems: "center", justifyContent: "center", borderRadius: 12, backgroundColor: palette.surface }, topIcon: { width: 36, height: 36, borderRadius: 12, alignItems: "center", justifyContent: "center", borderWidth: 1, borderColor: palette.line }, title: { color: palette.text, fontSize: 25, fontWeight: "900", marginTop: 3, letterSpacing: -0.6 }, hero: { flexDirection: "row", alignItems: "center", gap: 14, padding: 19 }, heroTitle: { color: palette.text, fontSize: 20, lineHeight: 24, fontWeight: "900", marginTop: 7 }, heroText: { color: palette.muted, fontSize: 12, lineHeight: 18, marginTop: 7 }, search: { height: 50, borderRadius: 15, borderWidth: 1, borderColor: palette.line, backgroundColor: palette.surface, flexDirection: "row", alignItems: "center", paddingHorizontal: 14, gap: 8 }, searchInput: { flex: 1, color: palette.text, fontSize: 13 }, filterRow: { gap: 8, paddingVertical: 1 }, filter: { borderWidth: 1, borderColor: palette.line, borderRadius: 999, paddingHorizontal: 13, paddingVertical: 9 }, filterActive: { backgroundColor: palette.mint, borderColor: palette.mint }, filterText: { color: palette.muted, fontSize: 11, fontWeight: "800" }, filterTextActive: { color: palette.ink }, listing: { gap: 15 }, listingTop: { flexDirection: "row", alignItems: "flex-start", justifyContent: "space-between", gap: 8 }, listingName: { flexDirection: "row", alignItems: "center", gap: 10, flex: 1 }, materialName: { color: palette.text, fontSize: 15, fontWeight: "900", lineHeight: 19, flexShrink: 1 }, muted: { color: palette.muted, fontSize: 11, lineHeight: 17 }, quantityRow: { flexDirection: "row", alignItems: "flex-end", justifyContent: "space-between" }, quantity: { color: palette.mint, fontSize: 27, fontWeight: "900", letterSpacing: -0.7 }, batchBlock: { alignItems: "flex-end" }, batchLabel: { color: palette.muted, fontSize: 8, letterSpacing: 1, fontWeight: "900" }, batch: { color: palette.text, fontSize: 10, fontWeight: "800", marginTop: 3 }, divider: { height: 1, backgroundColor: palette.line }, listingFoot: { flexDirection: "column", alignItems: "stretch", justifyContent: "space-between", gap: 10 }, disclaimer: { color: palette.amber, fontSize: 10, fontWeight: "800", marginTop: 3 }, actionButtons: { flexDirection: "row", alignItems: "center", justifyContent: "flex-end", gap: 7 }, passportButton: { height: 42, borderRadius: 13, borderWidth: 1, borderColor: palette.line, paddingHorizontal: 10, flexDirection: "row", alignItems: "center", gap: 5 }, passportText: { color: palette.mint, fontSize: 10, fontWeight: "900" }, note: { color: palette.muted, fontSize: 10, lineHeight: 15, fontStyle: "italic", marginTop: 1 }, empty: { alignItems: "center", gap: 8, paddingVertical: 30 }, emptyTitle: { color: palette.text, fontSize: 16, fontWeight: "900" }, emptyText: { color: palette.muted, fontSize: 12 }, modalBackdrop: { flex: 1, justifyContent: "flex-end", backgroundColor: "rgba(2,10,14,0.72)" }, modal: { backgroundColor: palette.inkSoft, borderTopLeftRadius: 28, borderTopRightRadius: 28, padding: 22, paddingBottom: 28, gap: 14, borderWidth: 1, borderColor: palette.line }, modalHead: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, modalTitle: { color: palette.text, fontSize: 25, fontWeight: "900", letterSpacing: -0.7 }, modalId: { color: palette.mint, fontSize: 12, fontWeight: "800", marginTop: -8 }, passportGrid: { flexDirection: "row", flexWrap: "wrap", gap: 15, borderTopWidth: 1, borderBottomWidth: 1, borderColor: palette.line, paddingVertical: 14 }, value: { color: palette.text, fontSize: 13, fontWeight: "800", marginTop: 3, maxWidth: 140 }, timeline: { flexDirection: "row", justifyContent: "space-between", paddingVertical: 4 }, timelineStep: { flex: 1, alignItems: "center", gap: 6 }, timelineDot: { width: 11, height: 11, borderRadius: 6, borderWidth: 1, borderColor: palette.line, backgroundColor: palette.surface }, timelineDone: { backgroundColor: palette.mint, borderColor: palette.mint }, timelineText: { color: palette.muted, fontSize: 7, fontWeight: "900", textAlign: "center" }, modalTitle2: { color: palette.text, fontSize: 14 },
});
